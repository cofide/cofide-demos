apiVersion: v1
kind: ServiceAccount
metadata:
  name: ping-pong-client
  labels:
    app: ping-pong-client
    type: exchange
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping-pong-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ping-pong-client
      type: exchange
  template:
    metadata:
      labels:
        app: ping-pong-client
        type: exchange
    spec:
      serviceAccountName: ping-pong-client
      containers:
        - name: ping-pong-client
          image: ${COFIDE_DEMOS_IMAGE_PREFIX}ping-pong-exchange:${COFIDE_DEMOS_IMAGE_TAG}
          imagePullPolicy: ${COFIDE_DEMOS_IMAGE_PULL_POLICY}
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
          env:
            - name: PING_PONG_MODE
              value: client
            - name: EXCHANGE_URL
              value: ${EXCHANGE_URL}
            - name: PING_PONG_SERVICE_HOST
              value: "${PING_PONG_SERVER_SERVICE_HOST}"
            - name: PING_PONG_SERVICE_PORT
              value: "${PING_PONG_SERVER_SERVICE_PORT}"
            - name: SERVER_SPIFFE_ID
              value: ${SERVER_SPIFFE_ID}
            - name: SPIFFE_ENDPOINT_SOCKET
              value: unix:///spiffe-workload-api/spire-agent.sock
          volumeMounts:
            - name: spiffe-workload-api
              mountPath: /spiffe-workload-api
              readOnly: true
      volumes:
        - name: spiffe-workload-api
          csi:
            driver: "csi.spiffe.io"
            readOnly: true
