apiVersion: apps/v1
kind: Deployment
metadata:
  name: loadgen
spec:
  template:
    spec:
      containers:
      - name: loadgen
        env:
        - name: COFIDE_CAPITAL_VERSION
          value: v1
        - name: PAYMENTS_API_KEY
          valueFrom:
            secretKeyRef:
              name: cofide-capital-secrets
              key: payments-api-key
